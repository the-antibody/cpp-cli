#include <iostream>
#include <stdlib.h>

void startup()
{
    // clear the screen
    system("clear");

    // print top & command starter
    std::cout << " -- (cpp-cli terminal) --" << std::endl;
    std::cout << ">> ";
}